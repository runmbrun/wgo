@using WGO.Models

@model List<Character>
@{
    ViewBag.Title = "Guild";
}

<h2>Guild Roster</h2>

@Html.Partial("_MVCGridToolbar", new MVCGridToolbarModel()
{
    MVCGridName = "GuildRoster",
    PageSize = true,
    ColumnVisibility = true,
    Export = true,
    GlobalSearch = true
})
@Html.MVCGrid("GuildRoster")
